streamlit==1.29.0 #den der laver webappen
pandas==2.0.3 #den der bruges til at arbejde med data(tabeller)
numpy==1.24.3 #den der bruges til bereegninger
matplotlib==3.7.1 #den der bruges til at lave grafer
requests==2.28.2 #den der bruges til at sende HTTP anmodninger

#requirments.txt filen indeholder de nødvendige Python pakker og deres versioner, som skal installeres for at køre  projektet. 

#opskrift på hvilke værktøjer mit program skal bruge